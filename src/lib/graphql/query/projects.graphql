query Projects {
  nhtblProjects(first: 100) {
    nodes {
      imageGallery {
        imageGallery {
          nodes {
            mediaType
            mimeType
            mediaItemUrl
            altText
            mediaDetails {
              sizes {
                sourceUrl
                width
                height
                name
              }
            }
          }
        }
      }
      content(format: RENDERED)
      slug
      uri
      title(format: RENDERED)
      featuredImage {
        node {
          altText
          mediaDetails {
            sizes {
              height
              width
              name
              sourceUrl
            }
          }
        }
      }
    }
  }
}